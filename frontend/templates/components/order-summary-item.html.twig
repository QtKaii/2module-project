<div class="flex items-center justify-between bg-white dark:bg-gray-800 p-4 rounded-lg shadow">
    <div class="flex-1">
        <h3 class="text-lg font-medium text-gray-800 dark:text-gray-200">{{ name }}</h3>
        {% if description %}
            <p class="mt-1 text-sm text-gray-500 dark:text-gray-400">{{ description }}</p>
        {% endif %}
    </div>
    <span class="ml-4 text-lg text-gray-600 dark:text-gray-300">€{{ price | number_format(2) }}</span>
</div>
